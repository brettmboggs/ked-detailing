{
  "name": "@ked/books",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bookkeeping rules: chart of accounts, balanced entries, bank CSV import and matching, reports.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings --test 'src/**/*.test.ts'"
  }
}
